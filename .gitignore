# User data â€” never commit someone's saved characters-in-progress
saves/

# Python
__pycache__/
*.pyc
*.pyo

# Windows .exe packaging (PyInstaller) â€” output only; HeroCompanion.spec IS the
# committed build recipe
build/
dist/
HeroCompanion-*.zip

# Not ours to publish â€” other players' hand-made master builds stay a private,
# local-only calibration corpus (see docs/architecture.md)
benchmarks/masters/

# Regenerable hub artifacts (sweep results are large and machine-specific)
benchmarks/*.jsonl
benchmarks/*.v1.jsonl
# Derived from the private masters corpus â€” stays local like the corpus itself
benchmarks/guyver_analysis.json
benchmarks/guyver_mxd_analysis.json

# Generated build exports (user artifacts)
exports/

# Shelved costume/3D experiment (kept local until it returns from the archive)
_archive_costume/
gamedata/

# Local session/tooling state
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
settings.json
gamelog/
champions_refresh_log.txt
champions_refresh_err.txt

data/inbox_key.bin
champions_refresh_*.txt
tools/gamedata/_wiki_cache/
