flask==2.3.3
requests==2.31.0
Werkzeug==2.3.7
pulp==3.3.2          # ILP constraint solver (server/solver.py); bundles CBC
Pillow==12.2.0  # required by pystray (tray icon) and Companion Lite's icon drawing
pystray==0.19.5      # tray icon for the packaged (windowed) app
