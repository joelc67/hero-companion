flask==3.1.3
requests==2.33.0
Werkzeug==3.1.8
pulp==3.3.2          # ILP constraint solver (server/solver.py); bundles CBC
Pillow==12.3.0  # Companion Lite only (pystray icon drawing) + dev-side art extraction tools
pystray==0.19.5      # Companion Lite's tray icon ONLY - the main app's tray died in 0.12.31
